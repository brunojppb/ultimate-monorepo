# Project Setup

Follow these instructions to get this app up & running.

## pnpm

Make sure that pnpm is installed. Follow the official docs [here](https://pnpm.io/installation).
Once this is done, install all dependencies with:

```shell
pnpm install
```

You can now run the web app in dev mode with:

```
pnpm dev:app
```
